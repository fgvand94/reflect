<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<title>Reflect</title>
	<link rel="stylesheet" type="text/css" href="http://localhost:5000/resources/css/user-page.css">
	<link rel="preconnect" href="https://fonts.googleapis.com">
	<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
	<link href="https://fonts.googleapis.com/css2?family=Caveat&display=swap" rel="stylesheet">

	<link rel="preconnect" href="https://fonts.googleapis.com">
	<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
	<link href="https://fonts.googleapis.com/css2?family=Parisienne&display=swap" rel="stylesheet">
</head>
<body>
	<main>
		<div class="background"></div>		
        <div class="username">
            <img src="{{obj.photo}}" class="user-photo">
			<h1>{{obj.person}}</h1>			
		</div>
		{{!-- I'll make this all look better later I'm just getting it all working now --}}
		{{#if obj.userMatch}}
		<div class="info">
			<h2 class="bio">Bio:</h2>
			<div class="bio-form">			
				<textarea class="bio-input" rows="5" cols="60"></textarea> <br>
				<h3 class="bio-button"><a>Update</a></h3>
				<h3 class="bio-cancel"><a>Cancel</a></h3>		
			</div>
			<h3 class="bio-text">{{obj.bio}}</h3>
			<h4><a class="update-bio">Update bio</a></h4>
			{{!-- I should maybe make it so all the pictures keep their aspect ratios
			Also need to fine to for responsiveness but I'll do that later. --}}
			<h2>Images:</h2>
			<div class="image-enlarge">
				<h2 ><a class="exit">x</a></h2>
				<img class="enlarged-image">
				<h3 ><a class="set-picture">Set profile picture</a></h3>
			</div>
			<div class="img-container">
			{{#each obj.photos}}
			<img src="{{this}}" class="photos">
			{{/each}}		
			</div>
			<h4><a  class="update-picture">add pictures<a></h4>
			{{!-- Look up how to make a small window pop up like how facebook 
			does. idk if I want to do it like that but it would be good to know --}}
			<div class="photo-form">
				<input type="file" class="photo-input" accept="image/png, image/jpeg"><br>
				<h3 class="photo-upload"><a>Upload</a></h3>
				<h3 class="photo-cancel"><a>Cancel</a></h3>
			</div>
			<h2 class="update">some other stuff:</h2>
		</div>
		{{else}}
		<div class="info">
			<h2>bio:</h2>
			<h3 class="bio-text">{{obj.bio}}</h3>		
			<h2>add pictures:</h2>
			{{!-- I need to make it so you can click and get a full size view
			of the picture.  --}}
			<h2>Images:</h2>
			<div class="image-enlarge">
				<h2 ><a class="exit">x</a></h2>
				<img class="enlarged-image">
			</div>
			<div class="img-container">
			{{#each obj.photos}}
			<img src="{{this}}" class="photos">
			{{/each}}
		</div>
		{{/if}}
	</main>
	{{!-- if I want the images to be enlargable when not logged in i'll either
	have to create a seperate js file for logged out or create more logic
	in my js file. I kind of like it not being linked at all when logged out
	though so idk I'll do that later.  --}}
	{{#if obj.userMatch}}
	<script src="http://localhost:5000/resources/js/user-page.js"></script> 
	{{else}}
	<script></script>
	{{/if}}
</body>
</html>